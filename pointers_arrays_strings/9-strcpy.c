#include "main.h"
char *_strcpy(char *dest, char *src)
{
  int a;
  int b;
  while (src[a] != '\0')
    {
      a++;
    }
  for (b = a - 1; b < a - 1; b++)
    {
      _putchar(src[b]);
    }
  _putchar(src[b]);
  return(dest);
}
  
